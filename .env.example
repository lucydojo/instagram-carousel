# Supabase (public)
NEXT_PUBLIC_SUPABASE_URL=https://yeitncbmdpbpehcrlzap.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_zCGnJcKAOmp93wFjop234w_RuuklDV8

# Supabase (server/admin) - optional but required for sending invites
# Never expose this key to the browser. Required for one-click first-run setup.
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# AI (server-only)
# Get a key at Google AI Studio and keep it secret (do NOT expose in NEXT_PUBLIC_*).
GEMINI_API_KEY=your_gemini_api_key
# Optional override (default is gemini-3.0-flash; accepts also "models/..." form)
GEMINI_MODEL=gemini-3.0-flash
# Optional override (default is gemini-2.5-flash-image; aka "Nano Banana")
GEMINI_IMAGE_MODEL=gemini-2.5-flash-image
